# API (read server)
fastapi
uvicorn[standard]

# data layer (shared by API + daemon): Postgres via psycopg 3 with a pooled
# connection. Selected through DATABASE_URL; see app/config.py.
psycopg[binary]>=3.2
psycopg_pool>=3.2

# Turnstile bot gate (API, see app/turnstile.py): itsdangerous signs the
# "solved the widget" cookie; httpx makes the async siteverify call.
itsdangerous>=2.1
httpx

# per-account password hashing (app/auth.py, scripts/create_researcher.py).
# Not wired into the running app -- the dashboard itself has no login, see
# app/turnstile.py -- kept for any future named-account feature.
argon2-cffi>=23.1

# ingestion daemon
requests
python-dotenv

# per-run code-diff signal (daemon only): APTED tree-edit distance. The episode
# segmenter is vendored in app/episode_engine/ and needs no third-party packages.
apted>=1.0

# tests (pytest tests/); pytest-cov for coverage (pytest --cov=app). Frontend
# tests live under frontend/ (npm test).
pytest
pytest-cov

# docs (mkdocs serve, also started by scripts/start.sh on :4000). Pulls in mkdocs
# and pymdown-extensions, which cover every extension in mkdocs.yml.
mkdocs-material

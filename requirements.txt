# API (read server)
fastapi
uvicorn[standard]

# data layer (shared by API + daemon): Postgres via psycopg 3 with a pooled
# connection. Selected through DATABASE_URL; see app/config.py.
psycopg[binary]>=3.2
psycopg_pool>=3.2

# researcher auth (API): argon2 password hashing; itsdangerous signs the session
# cookie behind starlette's SessionMiddleware.
argon2-cffi>=23.1
itsdangerous>=2.1

# ingestion daemon
requests
python-dotenv

# per-run code-diff signal (daemon only): APTED tree-edit distance. The episode
# segmenter is vendored in app/episode_engine/ and needs no third-party packages.
apted>=1.0

# tests (pytest tests/). httpx backs the FastAPI TestClient; pytest-cov for
# coverage (pytest --cov=app). Frontend tests live under frontend/ (npm test).
pytest
pytest-cov
httpx

# docs (mkdocs serve, also started by scripts/start.sh on :4000). Pulls in mkdocs
# and pymdown-extensions, which cover every extension in mkdocs.yml.
mkdocs-material

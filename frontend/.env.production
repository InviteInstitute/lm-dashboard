# Production build: call the SAME origin that served the page. FastAPI serves the
# built app and /api behind one Cloudflare tunnel, so an empty base URL keeps every
# request on that host (no CORS, no hardcoded localhost). api.js uses `??`, so this
# empty value is honored as "relative" instead of falling back to localhost:8000.
VITE_API_URL=
